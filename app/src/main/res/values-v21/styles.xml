<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="BaseThemeStyle">
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <!--Android 5.x开始需要把颜色设置透明，否则导航栏会呈现系统默认的浅灰色-->
    </style>

    <style name="AppTheme_StatusTrans" parent="BaseThemeStyle">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <!--Android 5.x开始需要把颜色设置透明，否则导航栏会呈现系统默认的浅灰色-->
    </style>

    <style name="ToolbarPopupTheme" parent="@style/Widget.AppCompat.Light.PopupMenu.Overflow">
        <!-- 是否覆盖锚点，默认为true，即盖住Toolbar -->
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownWidth">wrap_content</item>
        <item name="android:paddingRight">5dp</item>
        <!-- 弹出层背景颜色 这是一个colorStateList 否则不会有涟漪效果-->
        <!--<item name="android:popupBackground">@color/black</item>-->
        <!-- 弹出层垂直方向上的偏移，即在竖直方向上距离Toolbar的距离，值为负则会盖住Toolbar -->
        <item name="android:dropDownVerticalOffset">5dp</item>
        <!-- 弹出层水平方向上的偏移，即距离屏幕左边的距离，负值会导致右边出现空隙 -->
        <!--<item name="android:dropDownHorizontalOffset">0dp</item>-->
        <!-- 设置弹出菜单文字颜色 -->
        <!--<item name="android:textColor">@color/white</item>-->
    </style>

</resources>
