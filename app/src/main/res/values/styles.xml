<resources>

    <!-- Base application theme. -->

    <style name="BaseThemeStyle"  parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme" parent="BaseThemeStyle">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme_StatusTrans" parent="BaseThemeStyle">
        <!-- Customize your theme here. -->
    </style>

    <style name="textview_login">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="edittextview_login">
        <item name="android:textColor">@color/lightblue</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColorHint">@color/gray</item>
    </style>

    </resources>
